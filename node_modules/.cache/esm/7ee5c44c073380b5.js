let Model;_073‍.x([["fetchAll",()=>fetchAll],["saveNewPosts",()=>saveNewPosts],["updatePosts",()=>updatePosts],["deletePosts",()=>deletePosts],["default",()=>_073‍.o]]);_073‍.w("../models/model",[["default",["Model"],function(v){Model=v}]]);

       const fetchAll = ( next) =>{
    return Model.find({});
}

       const saveNewPosts = (data) =>{
    const post = new Model({
        pinned : data.pinned,
        flag : data.flag,
        isDeleted : "N",
        notes : data.notes,
        createdAt : new Date()
    });
    
    return post.save();

}

       const updatePosts = (post, data) =>{
        post.flag = data.flag;
        post.pinned = data.pinned;
        post.notes = data.notes;
        post.updatedAt= new Date();
        _073‍.g.console.log(post);
        return post;
    
}

       const deletePosts = (post) => {
    post.isDeleted = "Y";
    post.deletedAt = new Date();
    return post;
}

_073‍.d(fetchAll);